% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{get_repository_path}
\alias{get_repository_path}
\title{Get repo path}
\usage{
get_repository_path(
  repo_root,
  r_version,
  os,
  arch = NA_character_,
  cran_subpath = FALSE
)
}
\arguments{
\item{repo_root}{character}

\item{r_version}{character}

\item{os}{character}

\item{arch}{character x86_64 or arm64}

\item{cran_subpath}{(default FALSE) uses CRAN style subpaths}
}
\value{
character
}
\description{
Get repo path
}
\details{
R Universe has adopted \if{html}{\out{<os name>}}-\if{html}{\out{<arch>}} style path for linux and
and macosx, \if{html}{\out{<compiler>}}-\if{html}{\out{<arch>}} for windows. CRAN has not adopted these
conventions for linux and windows, so Bioconductor has not yet adopted them.
The \code{cran_subpath} parameter should be TRUE to not use R Universe subpath
for windows.

\code{r_version}, \code{os}, and \code{arch} are vectorized: pass vectors of equal length
(or length-1 values to be recycled) to resolve paths for multiple builds
in one call. \code{repo_root} is typically a single scalar shared across builds.
A \code{NA} \code{os} (no known platform for that row -- e.g. no binary built for it)
resolves to \code{NA_character_} for that row rather than an error or a
misleading \code{"src/contrib"} fallback.
}
\examples{
repo_root <- "/home/biocpush/PACKAGES/3.22/bioc"
get_repository_path(repo_root, "4.6.0", "windows", "arm64")
get_repository_path(repo_root, c("4.6.0", "4.6.0"), c("windows", "macosx"),
                    c("arm64", "arm64"))

}
